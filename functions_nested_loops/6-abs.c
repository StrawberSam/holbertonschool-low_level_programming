#include "main.h"
#include <stdio.h>
#include <stdlib.h>

/**
 * _abs - check the code
 *@r: the letter we need for the number
 * Return: absolute value.
 */

int _abs(int r)
{

	if (r < 0)
		return (-(r));

	return (r);
}
